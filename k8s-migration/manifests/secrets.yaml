apiVersion: v1
kind: Secret
metadata:
  name: sound-scout-secrets
  namespace: default
type: Opaque
data:
  # Placeholder template — do NOT put real values here, this file is committed.
  # Real secrets live in secrets.local.yaml (gitignored) or are created imperatively;
  # see k8s-migration/manifests/README.md.
  DATABASE_URL: ""
  WORKER_SECRET: ""
  GEMINI_API_KEY: ""
  # Required in production -- config/secrets.js refuses to start without these.
  ACCESS_TOKEN_SECRET: ""
  REFRESH_TOKEN_SECRET: ""
